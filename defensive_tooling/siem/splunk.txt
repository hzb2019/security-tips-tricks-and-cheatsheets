
  Splunk is one of the leading SIEM solutions in the market that provides the ability to collect,
  analyze and correlate network and machine logs in real-time.

  Splunk has three main components:
    Forwarder:
      Installed on the endpoint to be monitored, forwarder collects data and sends it back
      to the Splunk instance. Minimally impacts endpoint performance.
    Indexer:
      Processes data received from Forwarders into key:value pairs and determines data types.
    Search Head:
      Where users can search the indexed data using terms or Splunk Search Processing Language.

  
__________
SPLUNK 201

  Start by clicking on "Search and Reporting" on the left panel.

  Interesting log sources:
    wineventlog: It contains Windows Event logs
    winRegistry: It contains the logs related to registry 
                 creation / modification / deletion etc.
    XmlWinEventLog: It contains the sysmon event logs. It is a very important 
                    log source from an investigation point of view.
    fortigate_utm: It contains Fortinet Firewall logs
    iis: It contains IIS web server logs
    Nessus:scan: It contains the results from the Nessus vulnerability scanner.
    Suricata: It contains the details of the alerts from the Suricata IDS. 
              This log source shows which alert was triggered and what caused the alert 
              to get triggered— a very important log source for the Investigation.
    stream:http: It contains the network flow related to http traffic.
    stream: DNS: It contains the network flow related to DNS traffic.
    stream:icmp: It contains the network flow related to icmp traffic.

  All available log and other sources can be viewed in the 
  "Data Summary" tab found on the search tab.

  Before searching, make sure the timeframe is correct, and 
  the search mode is set to the intended value.

  Checking for reconnaisance phase activity:
    Look for IP addresses with heavy activity. When these are located, check things 
    like user agents, POST requests, URI's for suspicious activity. 

  Checking for exploitation phase activity:
    Search for requests by IP count using the given search query:
      index=botsv1 imreallynotbatman.com sourcetype=stream* | stats count(src_ip) as Requests by src_ip | sort - Requests
    
    Useful information can be found by searching for traffic for a specific destination IP, then
    checking from what source IP addresses traffic is coming from.
    
    Analyzing HTTP traffic:
      Try checking and filtering by HTTP method.

      Check interesting fields like "src_ip", "form_data", "http_user_agent", and "uri"

      Use the following query for a convenient table with useful info (make sure to filter for
      POST requests):
          |  table _time uri src_ip dest_ip form_data

      Extract password credentials with the following query:
        index=botsv1 sourcetype=stream:http dest_ip="192.168.250.70" http_method=POST form_data=*username*passwd* 
        | rex field=form_data "passwd=(?<creds>\w+)"  
        | table src_ip creds

  Checking for installation phase activity:
    Search for executables uploaded over HTTP:
      index=botsv1 sourcetype=stream:http dest_ip="192.168.250.70" *.exe
    Check the part_filename{} extracted field for the names of files found.

    To see if this file was executed, we can filter for the filename we are checking for
    executions of, then check what values exist for the "sourcetype" field. Seeing the file
    showing up in things like Sysmon is important for determining the extent of what it had done.

    Using our knowledge of Sysmon, we can use the "EventCode" field to filter for Event ID 1
    Sysmon logs for process creation events:
      index=botsv1 "3791.exe" sourcetype="XmlWinEventLog" EventCode=1

    Checking the "CommandLine" field we can filter the logs further to only be related to 
    values where the executable we are interested in is being run.

  Investigating actions on objectives:
    For this phase, looking through Suricata logs will be where we start. In particular,
    checking events where traffic is directed towards the server that was defaced will be 
    valuable. 

    We see heavy traffic from external sources (for some reason the writeup doesn't match
    this). If there was not inbound traffic from external sources, we could investigate
    the traffic between this server and the other internal addresses, or as the writeup
    chooses to do - change the direction of things and check for outbound traffic. 
      NOTE: it didn't match it because I was filtering by "dest_ip" rather than just "dest". 
      not sure what the difference is.
      NOTE 2: turns out when you check for outbound traffic you don't want to look for "dest"
      anymore now you do in fact want to look at "dest_ip" lol
      NOTE 3: as explained later in the problem, the "src" field actually indicates who
      initiates the connection to the given "dest" address. using "src_ip" and "dest_ip"
      technically still work, but it seems the nuance is that "src" indicates who initiated
      the TCP handshake or w/e, so seeing initiations come from the server side is unusual.

    When investigating the traffic on the destination addresses, we can look at things like
    the URL's being accessed to check for suspicious activity. A file appears to have been 
    uploaded/accessed, we can investigate where it came from using the following query:
      index=botsv1 url="/poisonivy-is-coming-for-you-batman.jpeg" dest_ip="192.168.250.70" 
      | table _time src dest_ip http.hostname url

    For the last problem, we can filter for sourcetype=fortigate_utm and src=40.80.148.42.
    From there, we can check the "signature" field and pull the one that includes the term
    "SQL" to find the answer we are looking for.

  Investigating C2:
    "The attacker uploaded the file to the server before defacing it. While doing so, 
    the attacker used a Dynamic DNS to resolve a malicious IP. Our objective would be to find 
    the IP that the attacker decided the DNS."
    I have no idea how one would determine this, I assume this is just provided as context.

    The problem recommends continuing to look through the firewall logs, and specifically
    searching for the name of the file that was uploaded:
      index=botsv1 sourcetype=fortigate_utm"poisonivy-is-coming-for-you-batman.jpeg"

    Very conveniently, there is only one URL associated with this traffic, and we can reasonably
    assume this is the C2 server. To be extra safe, we can correlate this with other log sources.
    Here we are checking the HTTP stream logs for the file between our server and the C2 server:
      index=botsv1 sourcetype=stream:http dest_ip=23.22.63.114 "poisonivy-is-coming-for-you-batman.jpeg" src_ip=192.168.250.70
    We can also check the DNS logs:
      index=botsv1 sourcetype=stream:dns "prankglassinebracket.jumpingcrab.com"
    We can see the DNS queries made to 8.8.8.8 for this URL. We can see in the logs responses
    to "name:prankglassinebracket.jumpingcrab.com" with "host_addr:23.22.63.114". If we wanted,
    we could investigate individual queries using the "transaction_id" field.

  Investigating weaponization and delivery involve using threat intel tools to further
  check the IP addresses and files that were detected to determine extent of damage and 
  ensure everything is properly cleaned up. Hybrid analysis (hybrid-analysis.com) has 
  useful insights into malware behavior and details found there can be used to check logs
  for further detections of malware that may be remaining. 


    


______
TABLES

  To create a table, use the keyword "table" and follow it with the intended columns

  Examples:
    table _time uri src_ip dest_ip form_data

_____
REGEX

  To use regex, use the keyword "rex", type the field=[FIELD] to search in, then add a regex 
  in quotes. Extractions can be performed using (?<EXTRACTION>) which can then be accessed
  subsequently in the query.

  Examples:
    rex field=Local ".*:(?<port>.*)"
    rex field=form_data "passwd=(?<creds>\w+)" | table src_ip creds